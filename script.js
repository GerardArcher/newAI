// Get the header element
let header = document.querySelector('header');

// Add a click event to the header
header.addEventListener('click', function() {
    header.textContent = "Hello from MyWebsite!";
});
